# Local backend (Expo dev)
EXPO_PUBLIC_API_BASE_URL=http://localhost:4000

# Staging/production API (set before `eas build` or `npx expo export -p web`)
# EXPO_PUBLIC_API_BASE_URL=https://rack-n-roll-api.onrender.com
